{{R3M}}
<td scope="row" class="actions">
    {{if($nr > 0)}}
        <i class="fas fa-chevron-up"></i><br>
    {{/if}}
    {{$nr}} / {{$count}} <br>
    {{if($nr < $count)}}
        <i class="fas fa-chevron-down"></i><br>
    {{/if}}
</td>