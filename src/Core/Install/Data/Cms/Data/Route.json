{
    "{$subdomain}-{$host}-{$extension}-index": {
        "path": "\/",
        "host": [
            "{$subdomain}.{$host}.{$extension}"
        ],
        "controller": "Host.{$subdomain|uppercase.first}.{$host|uppercase.first}.{$extension|uppercase.first}.Controller.Index.overview",
        "method": [
            "GET",
            "POST"
        ]
    }
}